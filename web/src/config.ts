/** Default city for prayer-time display. Placeholder until wired to a real prayer-time source (see handoff README). */
export const KOTA = 'Banda Aceh, Aceh';

/** Static placeholder Hijri date — not calculated. Needs a real Hijri conversion in production. */
export const HIJRI_STR = '± 8 Muharram 1448 H';
